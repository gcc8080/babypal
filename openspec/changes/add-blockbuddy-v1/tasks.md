> 阶段依赖：P0 → P1 →（P2 → P3、P2 → P4、P5）→ P6。P1 是全项目地基，五个内容模块全部构建其上；P4 的部件加法直接复用 P2 的合体交互，不重复实现。第 3 组内容流水线与 P1 起并行，不占关键路径。
>
> 工期告急时的砍单顺序：家长看板 → 反义词跷跷板 → 大小写配对 → 百格板。绝不砍：积木引擎手感、生日彩蛋、家人相册。

## 1. P0 脚手架（约 3 天）

- [x] 1.1 把 `.fvmrc` 的 flutter 从 `3.27.4` 改为 `3.44.8`，本机执行 `fvm install` 并确认 `fvm flutter --version` 输出 Dart 3.12.2
- [x] 1.2 在仓库根执行 `flutter create --platforms=android,ios --org <reverse-domain> .`，保留既有 `openspec/`、`.github/`、`.fvmrc` 不被覆盖
- [x] 1.3 从 `.gitignore` 删除 `pubspec.lock` 一行（本项目是应用不是 library，锁文件须入库）
- [x] 1.4 在 `pubspec.yaml` 加入依赖：`flutter_riverpod ^3.3.2`、`flutter_soloud ^4.0.13`、`record ^7.1.1`、`shared_preferences ^2.5.5`、`path_provider ^2.1.6`、`wakelock_plus ^1.7.0`、`flutter_svg ^2.3.0`，执行 `fvm flutter pub get` 并提交 `pubspec.lock`
- [x] 1.5 确认平台最低版本无需显式配置：实测 Flutter 3.44.8 默认 minSdk **24** / iOS **13.0**，已高于全部依赖要求（`record_android` 23、`flutter_soloud` 21、`record_ios` 12.0、`flutter_soloud` iOS 13.0）。`build.gradle.kts` 保持 `minSdk = flutter.minSdkVersion` 不写死数字
- [x] 1.6 三处声明横屏锁定：`lib/main.dart` 的 `SystemChrome.setPreferredOrientations`、`AndroidManifest.xml` 的 `android:screenOrientation="sensorLandscape"`、iOS `Info.plist` 的 `UISupportedInterfaceOrientations` 只留横屏两项
- [x] 1.7 在 `Info.plist` 加 `NSMicrophoneUsageDescription`，文案说明仅用于家长录音与生日吹蜡烛；确认 `AndroidManifest.xml` 与 `Info.plist` 均**不含**任何网络权限声明
- [x] 1.8 建立 `lib/core/design/tokens.dart`：设计基准短边 360dp、缩放因子 `shortestSide / 360`、可拖拽积木与按钮下限 90dp、放置区下限 60dp、吸附半径系数 1.5、原创配色表
- [x] 1.9 在 `lib/main.dart` 接入 `wakelock_plus` 前台常亮并在退到后台时解除
- [x] 1.10 新建 `.github/workflows/ci.yml`（仓库当前无任何 workflow）：ubuntu job 固定 Flutter 3.44.8 跑 `flutter analyze`、`flutter test`、`flutter build apk --debug`；macos job 跑 `flutter build ios --no-codesign`
- [x] 1.11 选定生日交付用的 Android 真机（有多台时优先屏幕较大的一台），开启开发者选项与 USB 调试，跑通 `fvm flutter run -d <android>` 与 `fvm flutter install --release` 两条链路

> 实测设备：**MI 8 SE**（Android 9 / API 28 / arm64）。可用区域 2029×1080 物理像素，dpr 2.75 → 横屏逻辑尺寸约 **738 × 393 dp**，短边 393dp，`BlockScale = 1.09`。两条链路均已跑通，release APK 49.2 MB，启动无崩溃。
>
> **已确认以此机作为交付与调试设备。** 该机为 5.88 寸小屏，与 D12「有多台时优先屏幕较大的一台」的建议不符——这是**已知取舍**，非疏漏。相应地，后续设计与验收一律按 393dp 短边把关：积木必须在此尺寸下仍然好抓（抓取目标 ≥90dp、放置区 ≥60dp），一屏可容纳的积木数量按此估算。日后若换大屏平板只会更宽松，不会失效。
>
> 构建环境问题与修复：NDK 28.2 搭配 Android SDK 自带的 CMake 3.22.1 在 macOS 上不传 `--target=` 三元组给 clang，导致其误选 Mach-O 链接器 `ld64.lld`（报 `unknown argument '--build-id=sha1'`），`flutter_soloud` 原生编译失败。已在 `android/build.gradle.kts` 中仅对 macOS 覆盖 CMake 版本为 4.0.2。
- [ ] 1.12 确认 iOS 侧能力保留：本机 `fvm flutter run -d <ios-simulator>` 可跑，CI 的 `flutter build ios --no-codesign` 通过。iOS **不做真机验收**，回归职责全部由 CI 承担
- [x] 1.13 确认代码中不存在 Android 专有分支，保留日后取得开发者账号后直接上 iOS 真机的能力

## 2. P1 积木引擎与地基（约 1 周，本阶段做扎实，后面五个模块全靠它）

- [x] 2.1 `lib/core/block/block_model.dart`：`BlockBody` 数据模型（id、尺寸、颜色、表情、所属群组）
- [x] 2.2 `lib/core/block/snap_grid.dart`：网格吸附纯逻辑——最近合法格位判定、1.5 倍宽容半径、多候选取最近、已占用格位跳到最近空闲格位
- [x] 2.3 `test/core/block/snap_grid_test.dart`：覆盖半径内、半径外、多候选、已占用、边界共 5 类场景
- [x] 2.4 `lib/core/block/block_face.dart`：`CustomPainter` 程序化画脸——待机眨眼、开心、惊讶三种表情，零素材
- [x] 2.5 `lib/core/block/block_widget.dart`：单块渲染 + 手势 + 挤压回弹；保证每一次触摸都触发动画与音效（音效通过 `onSquash` 回调预留，待 2.9 音频总线接入）
- [x] 2.6 `lib/core/block/block_board.dart` 拖拽通道：拖拽、落位吸附、手指移出屏幕边缘按释放处理、多指同时拖拽互不干扰
- [x] 2.7 `lib/core/block/block_board.dart` **点选通道**：点积木进入选中态（含选中态视觉）→ 点目标位飞入落子；再点积木或点空白取消选中。此为全局不变量，五个模块不得只实现拖拽通道
- [x] 2.8 `lib/core/block/block_board.dart` 合体、分裂与群组整体拖动，三者均带过程动画

> 实现说明：状态与规则拆到 `lib/core/block/block_board_controller.dart`，`block_board.dart` 只负责渲染与指针事件。这样合体/分裂/群组/吸附落位可以用纯逻辑单测覆盖（`test/core/block/block_board_controller_test.dart`，15 个用例直接对应规格场景），不需要 pump 组件树。
- [x] 2.9 `lib/core/audio/audio_bus.dart`：`flutter_soloud` 封装——音效池（并发短音效）+ 语音队列（同一时刻一条，支持排队/打断两种策略）
- [x] 2.10 `lib/core/audio/voice_resolver.dart`：文档目录 `voice_overrides/<key>.wav` 优先，回落 `assets/audio/<key>.wav`；覆盖文件损坏时必须回落而非静默。扩展名抽为单一常量
- [x] 2.11 `test/core/audio/voice_resolver_test.dart`：有覆盖、无覆盖、覆盖文件损坏三种优先级场景
- [x] 2.12 `lib/core/content/models.dart` 与 `pack_loader.dart`：四类内容模型 + `schemaVersion` 校验（过高则跳过整包）+ 单条目非法时局部跳过而不丢弃整包
- [x] 2.13 `test/core/content/pack_loader_test.dart`：版本受支持、版本过高、缺 schemaVersion、单条目字段缺失、合体字引用不存在的部件
- [x] 2.14 `assets/packs/numbers.json` 首个内容包，跑通「内容包 → 加载器 → 积木」全链路

> 实现说明：
> - `parse()` 是**同步纯函数**（输入 JSON 字符串，不碰 IO 与 AssetBundle），因此两级降级路径全部可直接单测。
> - 汉字**分两趟解析**：先收集全部合法条目，再校验合体字的 `parts` 是否都在包内。一趟扫描会把「部件定义在合体字之后」误判为缺失（已有用例覆盖）。
> - `ContentPack.skipped` 保留被跳过条目及原因而非静默丢弃——否则一个拼错的字段会让某个字安静地从 App 里消失。
> - `numbers.json` 逐条列出 0–100 共 101 条而非在代码里生成：`allVoiceKeys` 必须能枚举出全部 202 个中英语音键，否则 `tool/gen_audio.dart` 无从得知要合成哪些音频。
> - 全链路验证 `test/core/content/numbers_pack_pipeline_test.dart` 直接读真实 asset 文件，验到「十条逐行落满 10×10 百格板」。
- [x] 2.15 `lib/core/progress/`：`shared_preferences` + JSON 存档读写
- [x] 2.16 `lib/modules/home/`：方块星球首页，纯图形入口、零文字

> 实现说明：
> - `ProgressStore` 的时长统计基于**墙钟差值**，在生命周期切换点累加落盘（D7）。另提供 `flush()` 供长时间连续游玩时定期落盘——**计时依据仍是墙钟，定时器只负责触发落盘，不参与计数**，这样进程被强杀最多只丢一个落盘间隔。已覆盖切后台、杀进程、跨日、系统时间回拨四类场景。
> - `weakestItems()` 只挑**练过的**条目：没练过属于「还没教」而非「没掌握」，混在一起会让家长看板变成一份无用的长名单。同理 `accuracy` 在没练过时返回 null 而非 0。
> - 首页入口改用原始 `Listener` 而非 `GestureDetector`：`onTapDown` 要等手势竞技场裁决，而外层横向滚动识别器会参与竞争，导致**按下要等一下才有形变**，违反「触摸必有回应」红线。测试专门锁住这条。
> - 触控下限用**绝对 90dp**，不乘缩放因子——dp 已是密度无关单位，90dp 对应手指物理尺寸；小屏上再乘 0.85 会变成 76dp，恰好击穿这条红线要守的东西。缩放只能放大入口。
>
> 顺带修复：`BlockFace` 的眨眼调度原用 `Future.delayed`，其 Timer 无法取消，组件销毁后仍会挂最多 6 秒。拼搭台上积木频繁增删会攒下一堆悬空定时器。已改为可取消的 `Timer` 并在 `dispose` 中取消。
- [x] 2.17 真机手感验收：连续快速点击 10 个积木音效不丢不卡；手指移出边缘不崩；两指同时拖拽正常；手机与平板横屏与缩放均正确

> 2026-07-26 在 MI 8 SE（release 构建）上验收，结论「没啥大问题」。
>
> **已验**：拖拽吸附、挤压回弹、多指拖拽、边缘移出、点选通道、切后台恢复、横屏锁定与 393dp 短边缩放。
>
> **音效补验（2026-07-26 二轮）**：`tool/gen_sfx.dart` 程序化合成 6 个音效后接入沙盒。真机确认 soloud 初始化成功、输出流 `state:started`（`dumpsys audio` 中 OpenSL ES AudioPlayer 挂在本应用 pid 上）、连续快速点击无异常日志。人耳验收结论「音效听着没问题」——连点不丢音、`return` 未被听成失败音、音量与节奏均可接受。**2.17 至此全部验完，无遗留项。**
>
> **未验 — 平板**：交付设备已定为 MI 8 SE，手上无 Android 平板，「手机与平板」中的平板一侧只有 widget 测试覆盖（`home_page_test.dart` 用 320×240 与 738×393 两种尺寸），无真机验证。
>
> **待手感微调的猜测值**（均无依据，凭直觉定，日后按孩子实际反应调整）：拖拽判定阈值 14dp、眨眼间隔 2.2–6.0s、挤压幅度 0.88、回弹曲线 `elasticOut`。

## 3. 内容流水线（与 P1 起并行，不占关键路径）

- [x] 3.1 `tool/gen_audio.dart`：读内容包枚举 `voiceKey` → 调本机 TTS → 输出 **WAV（22.05 kHz 单声道 16-bit）** + manifest。macOS 命令形如 `say -v <voice> --file-format=WAVE --data-format=LEI16@22050 -o out.wav`（中文音色见下方 2026-07-27 的修正，英文用 `Samantha`）
- [x] 3.2 跑 `gen_audio.dart` 产出约 400 条 TTS 音频（约 26 MB），在 `pubspec.yaml` 声明资源目录
- [ ] 3.3 `tool/fetch_openmoji.dart`：按内容包里的 emoji 码点抽取 OpenMoji SVG 子集（约 150 个），输出到 `assets/icons/`
- [ ] 3.4 在 `assets/icons/` 放置 OpenMoji 的 LICENSE 文件（CC BY-SA 4.0），记录署名信息供家长区署名页使用
- [x] 3.5 校验：内容包中每个 `voiceKey` 在音频 manifest 中都有对应文件，缺失项列出清单

> 3.1/3.2/3.5 已完成（2026-07-26）：212 条 TTS 音频，实测 **6.6 MB**——远低于 design.md 估算的 26 MB。格式经测试校验为 WAV 单声道 / 22050 Hz / 16-bit。
>
> **中文音色 Tingting → Flo（2026-07-27，人耳发现的真 bug）**
>
> 人耳反馈「中文『三』听着像英文字母 N，但『四』和英文都正常」。查下来是真问题，不是错觉：
>
> | 「三」的声母 /s/ | 2–6 kHz | 8–11 kHz |
> |---|---|---|
> | Tingting | 0.2% | **98.4%** |
> | Flo | **98.7%** | 0.0% |
>
> Tingting 的 /s/ 几乎全部挤在 8–11 kHz，紧贴 22.05 kHz 采样率的 **11.025 kHz 奈奎斯特上限**，其中 24% 在最后 1 kHz。那个位置任何高频衰减都会抹掉声母，`sān` 剩 `an`，正是字母 N 的读音。**为什么只有「三」暴露**：Tingting 的「三」鼻音尾与元音等长（各 120ms），而「四」没有鼻音尾，掉了 /s/ 只剩 `ì`，不像任何熟悉的音。交付设备 MI 8 SE 是 5.88 寸机，小喇叭 9 kHz 上基本没输出——**只提采样率治不了根**（那只解决贴天花板，解决不了喇叭衰减），换音色才行。
>
> 换音色**必须**连带处理两件事，否则会引入新毛病：
>
> 1. **裁首尾静音**。新一代音色词尾留约 520ms 静音。语音队列要等每条播完才放下一条，「三·加·二·等于·五」白白多出 2.6 秒，一句话拖成一段等待。现已统一压到 60–75ms。裁剪阈值取满量程 0.4%、前端留 40ms 余量——**擦音起音本来就轻**（Tingting 的 /s/ 起始只有满量程 0.7%），阈值定高就会切掉声母，那正是要修的 bug。
> 2. **峰值归一化到 22000**。中英是连着放的两个声道，原先中文均值 17030、英文 22003，最低的中文只有 9718（低 10 dB），听感一句响一句闷。现中文全部对齐到英文水平，**英文文件未改动**。
>
> WAV 后处理必须**逐块遍历 RIFF**：`say` 在 `fmt ` 与 `data` 之间插了 4044 字节的 `FLLR` 对齐填充块，按固定 44 字节头读会拿到一堆零。产出时重写为规范 44 字节头。
>
> 音色不再写死显示名：新音色的显示名是本地化的（中文系统「Flo (中文（中国大陆）)」，英文系统「Flo (Chinese (China mainland))」），写死会在另一台机器上以一句 `Voice not found` 失败。现按「名字前缀 + 语言标签」从 `say -v '?'` 解析。
>
> **人耳复验通过**（用真资源按 `AudioBus` 的实际方式串播）：`三加二等于五`、`five is made of two and three`、`三 四 十` 连读、`十个一是一个十` 均无问题。**4.4 遗留的「词片拼播连贯度」与「单字音准」两条至此一并验完。**
>
> 数字读法由工具从 `value` 推导，因此 `numbers.json` 无需逐条写朗读文本（`21 → 二十一 / twenty-one`、`100 → 一百 / one hundred`，已抽查）。**名词的朗读文本无法推导**：`NounItem` 目前不携带文本字段，工具会报告缺失而非猜测——生成一条读错的音频比没有更糟。设计 `nouns.json` 时需补 `text` / `textEn` 字段。字母音素（/æ/）文本 TTS 无法可靠合成，留给真人录音覆盖。
>
> 3.5 的校验做成了 `test/content_audio_coverage_test.dart` 而非仅是脚本，让 CI 兜住：内容包加了新字却忘了跑 `gen_audio.dart`，构建阶段就该发现。
>
> **3.3 / 3.4 被阻塞**：OpenMoji 抽取依赖内容包里的 emoji 码点，而 `nouns.json` 尚未设计（属 P2–P3）。
>
> **音效已补**（超出 3.x 原定范围）：新增 `tool/gen_sfx.dart` 程序化合成 6 个音效（tap / pickup / snap / merge / split / return），共 42 KB。选择程序化而非引入外部音效包，理由与积木的脸一致——参数可调、零许可问题、风格自洽。全部音高取自五声音阶：快速连点时多个音效会叠在一起，五声音阶保证怎么叠都不难听。
>
> `return`（退回原位）**刻意不是失败音**：柔和下行小三度、音量压到 0.5，听起来像「它自己回家了」而不是「你错了」——无挫败红线在音效层的体现。

## 4. P2 数字 + 加法（约 1.5 周）

- [x] 4.1 `lib/modules/numbers/`：十条与单块积木，位值拆解玩法（23 = 2 十条 + 3 单块），支持 10 个单块合体为 1 个十条
- [x] 4.2 `test/modules/numbers/place_value_test.dart`：位值拆解纯逻辑（23 → 2 tens + 3 ones，整十数，边界值）

> 实现说明（4.1–4.2）：
>
> **判定只看总数，不看摆法。** `PlaceValueAttempt.matches()` 比的是 `rods*10 + units`，位置、顺序、十条与单块的比例一律不判——这是「十条与单块可互换」这条场景的直接实现，也让「摆在哪儿」彻底退出正误判定。
>
> **由此推出两处交互取舍**：① 位置既然不判定，就没有任何理由让他去瞄准落点，所以**点一下托盘的「源」，积木自己飞到合适位置**（`nextFreeAnchor`：十条从上往下一行一条，单块从下往上填，两者相向生长天然分区）；② 选中态在这里没有意义，所以**点已放置的积木 = 收回**。为此给引擎加了 `BlockTapHandler` 钩子（`onBlockTapped` 返回 true 即消费该点击）。**点选通道由托盘源承担，2.7 的全局不变量未被削弱。**
>
> **换十是演示不是纠错**：攒够 10 个单块先被接受，隔 700ms 再合成 1 个十条。不能立刻换——他刚放下第 10 块手还在那儿，画面立刻变会被理解成「我弄坏了」。
>
> **格边长由「十条必须完整摆下」倒推**：10 格宽的长条一旦超出屏幕，位值这一课就不成立。交付设备 738×393dp 上实测约 68dp/格（高于放置区下限 60dp）；短边 360dp 的机型降到约 60dp，仍在线上。托盘里每个**主动操作对象**一律 ≥90dp，已由用例锁住。
>
> ~~换十与庆祝的播报暂借数词音频~~ → 已随 4.4 补齐：换十播报「十个一，是一个十」，答对播报中英双声道数词。
>
> 33 个新用例（21 纯逻辑 + 12 组件），`flutter analyze` 干净。**真机已验（2026-07-27）**：源出块、自动落点（十条从上、单块从下相向生长）、答对变绿 + 笑脸 + 下一题点亮，全部正确。
- [x] 4.3 百格板：10×10 网格作展示面与落点区域，孩子拖动的是 ≥90dp 的十条或单块，**不得要求点中单格**；每填满一行放烟花

> 实现说明（4.3）：
>
> **网格是一张画，完全不接受触摸。** 规格「MUST NOT 要求点中单个格子」不是可用性建议，而是几何事实：手机横屏可用高度约 280dp，10 行就是每格 28dp，怎么排都不可能满足 90dp。结论不是「百格板做不了」，而是**格子根本不是操作对象**——他碰的是托盘里的十条与单块，格子只负责显示。有用例直接戳网格正中间断言计数不变。
>
> **只存「填了多少」一个计数，不存每格是谁填的。** 他放进去的十条与单块本来就没有身份，`10` 不会因为是一条十条还是十个单块凑出来的而不同。位值的表达差异已由 4.1 讲透，百格板讲的是另一件事：**100 是十行十列**。
>
> **整行与零散格上不同的颜色**（十条色 / 单块色）：34 看起来就是「三行十个」加「四个」，位值不用讲，看一眼就在那儿——4.1 那一课的回音。有用例锁住 `completedRows/looseCells` 与 `PlaceValue.tens/ones` 一致，不允许出现第二套答案。
>
> **不提供「百板」**：一步填完 100 会把这个模块变成一次点击，而它的乐趣恰恰在于一行一行爬上去、听着数字一个个报出来。播报一律 `interrupt`——连着按时只该听见最新那个数。
>
> 烟花走程序化绘制（`CustomPainter` + 粒子），与积木的脸、音效同一个理由：零素材、零依赖、参数可调。每行的抖动用行号做种子，同一行反复放形状一致。
>
> **踩到的坑**：烟花的 `AnimationController` 原本用 `late final` 惰性初始化，结果**一行都没填满就退出页面时**，`dispose()` 里的 `_burst.dispose()` 成了它的首次访问，于是在 dispose 期间才去 `createTicker`——那要查祖先节点，此时组件树已失活，直接抛异常。改到 `initState` 里建。这是真 bug 不是测试假象。
>
> 数字模块现有两个玩法（位值工作台 ↔ 百格板），与加法模块同样用图标按钮互相 `pushReplacement`。托盘里的积木缩略图与取色抽到 `lib/modules/numbers/piece_glyph.dart` 共用。
>
> 27 个新用例（16 纯逻辑 + 11 组件），全量 264 通过。**真机已验（2026-07-27）**：10×10 网格、大数字、玩法切换均正常。
- [x] 4.4 `lib/modules/addition/`：合体求和，两组积木拼接合体并播报算式

> 实现说明（4.4）：
>
> **音频走词片拼播，不做整句。** 10 以内的加法有 45 个有序算式、分解也有 45 个，整句要 180 条音频约 16 MB；词片只要 8 条连接词（加/等于/和/可以分成 × 中英）约 0.2 MB，还能拼出任意算式——他已经在数 100 了，迟早报到 10 以外。**决定性的理由是家长录音**：覆盖层的全部意义是「录四十来条就能换成爸妈的声音」，词片方案下录一次「加」覆盖全部算式，整句方案要录 45 句，覆盖层就名存实亡了。`AudioBus` 的语音队列本来就是为连续播报设计的（`VoicePolicy.queue` 的原注释即如此），新增 `speakSequence()` 串起来，词片间天然留一个小停顿——对正在学的孩子比连读更清楚。唯一的例外是「十个一，是一个十」：拼不出来且只有一句，整句生成。→ `lib/core/audio/narration.dart`，键的齐备性由 `content_audio_coverage_test` 兜住。
>
> **出题只报两个加数，不报得数**——报了他就没事可做了。合体成功后才报完整算式，中英各一遍。
>
> **合体有三条触发路径，全部保留**：① 拼到一起（同一行、边挨着边）② 叠到一起 ③ 点选通道点两下。3 岁的手不稳，多一条路就少一次挫败。规格原文说的是「两组积木**拼接**合体」，路径 ① 才是字面意思，也最好操作——把 A 放到 B 旁边比放到 B 正上方容易得多。
>
> **引擎收敛**：三条路径共用新增的 `BlockBoardController.mergeBlocks()`，`endDrag` 一并改为调用它。合体是有语义的动作（3+2=5、木+木=林），让三条路径各写一遍必然行为不一致。「拼到一起」的几何判定抽为 `blocksAreJoined()`，P4 的部件加法直接复用。
>
> **两个加数起始分置首尾两行**：放同一行会让凑十题（6+4 = 10 格）一开局就自己贴住，等于替他做完了。
>
> 托盘控件（`PressableTile` / `RoundActionButton`）抽到 `lib/core/design/controls.dart`，与位值工作台共用，P3–P4 继续复用。
>
> 32 个新用例（10 播报 + 11 组件 + 11 引擎），全量 189 通过。**真机已验（2026-07-27）**：点选通道合体正确，合体后 happy 表情（眯眼）、等式条填入得数、下一题点亮。
>
> **人耳已验（2026-07-27）**：词片拼播的连贯度与单字音准均通过——但这一验收顺带查出中文音色 Tingting 读「三」有缺陷，已整体换为 Flo，详见第 3 组的说明。**真机手感仍未验**（设备未连接）。
- [x] 4.5 反向分解：对总数积木执行分裂，生成两个加数并播报「五可以分成二和三」
- [x] 4.6 `test/modules/addition/decompose_test.dart`：分解枚举（5 → `1+4,2+3,3+2,4+1`）与边界（1 → 空集）

> 实现说明（4.5–4.6）：
>
> **交互只有一条规则：点哪儿切哪儿。** 手指落在积木的哪个位置，就从最近的那道格缝切开。`cutIndexAt()` 的 `clamp` 保证结果永远落在 `[1, total-1]`——点在最左端切出 `1+4`，点在最右端切出 `4+1`，点到积木外面也照切。**反向分解里不存在「没切中」**：点在积木上的任何位置都必须切出点什么来，否则就是「点了没反应」。
>
> 原计划的「拖一刀切开」改成了点击。竖划手势会和拖动积木抢同一条指针路径（`_dragSlop` 之上就是拖拽），而点击既无歧义又更宽容；拖拽因此留给挪位置与合回去。
>
> 为此把 `BlockTapHandler` 扩成 `(block, localPosition)`。落点直接取 `_PendingTouch.startPosition`——`Listener` 的坐标原点就是积木左上角，那个值天然就是「点在积木的哪个位置」，没有额外换算。
>
> **切开后点任意一块就合回去**（拖一块到另一块上也行），于是「切开—合回—换个地方再切」成为一个能一直转的循环，也省掉了一个「重来」按钮。合回去时播报的是**加法算式**——分解与合体因此是同一件事的两个方向，不是两个玩法。配色也共用：绿 = 总数，珊瑚 / 紫罗兰 = 两个加数。
>
> **已发现的分法记在托盘上**（一排小格）。这是这个模块唯一的驱动力，对应真实验收指标②「主动重复」。**它不是关卡**：找不齐没有任何后果，「下一个」任何时候都能按，只是找齐后会点亮。
>
> 两个玩法之间用一个图标按钮互相 `pushReplacement`，返回键始终直接回星球地图，不会越按越深。
>
> 30 个新用例（17 纯逻辑 + 13 组件），全量 218 通过。**真机已验（2026-07-27）**：点在第 2 与第 3 格之间切出 `5 = 2 + 3`，发现标记点亮，与规格场景一致。
- [x] 4.7 等式槽 `[?][+][?][=][?]`，槽位同时支持拖拽与点选双通道
- [x] 4.8 答案不符预期时用积木演示正确结果后允许重试，无红叉、无扣分、无倒计时

> 实现说明（4.7–4.8）：**两条是同一个功能**——规格「等式槽」需求下的「填入错误答案」场景写的就是 4.8，分开做会把同一段状态机拆成两半。
>
> **答对答错走同一条路：都用积木演示一遍。** 填了任何一个答案，两个加数都会真的合起来给他看，并播报完整算式。答错时演示完清空槽让他再试，答对时停住并点亮「下一题」。这样设计的关键在于——**「演示」因此不是一个错误信号**，它只是「答案长什么样」。若只在答错时演示，那个动画就成了变相的红叉。
>
> 界面上没有任何否定信号：不变红、不出叉、不扣分、不计次数、不倒计时。答错三次与答错零次，界面状态完全一致（有用例锁住）。
>
> **积木区是一台随手可用的算盘，不是装饰。** 两个加数一直摆在那儿，他可以自己把它们拖到一起合体、数出得数，再去选答案。**这不算作弊——用积木算出来正是我们想要的行为**，只是没人逼他。自己合体不会自动填槽：选哪个答案始终是他的决定。
>
> **候选答案的位置逐题轮换**（`round % 3`），干扰项紧贴正确答案（±1）。固定位置他会改记位置，那就变成记位置而不是算加法；干扰项差得远则一眼可排除，等于没考。
>
> 双通道：**点候选直接填进槽**（只有一个槽，无歧义，比「先选中再点槽」少一步），**拖候选进槽**同样可以（`Draggable`/`DragTarget`，拖到槽上方时槽长大一圈给明确信号）。候选块外层套一个 `Listener` 只管「按下立刻挤压」，不消费事件，点击与拖拽的归属仍由手势竞技场正常裁决。
>
> **踩到的坑**：`_onBoardChanged` 里无条件 `setState` 会崩——`updateGrid` 是在 `LayoutBuilder` 的 builder 里调的，监听里一 `setState` 就是「build 期间标记重建」。这个页面本就不需要监听积木状态（等式条与托盘都不依赖它），直接不挂监听即可。
>
> 三个玩法接成一个环：**合体 → 分解 → 等式 → 合体**，图标画的是下一站，一律 `pushReplacement`，返回键始终直接回星球地图。
>
> 17 个新用例，全量 235 通过。**真机已验（2026-07-27）**：候选位置轮换正确（首题正确答案在第一位），答错时槽显示他填的数（中性白、无红叉）、积木合成真实得数并笑、候选变灰，3.4 秒后复位干净、零痕迹。
- [x] 4.9 中英双声道播报：同一算式依次中英播报，共用同一套内容与关卡

> 4.9 随 4.4/4.5/4.7 一并落地：`Narration.bilingual*` 一律「先中文后英文」，两个声道由**同一个构造函数**生成、只换语言前缀（有用例锁住「去掉前缀后两串完全一致」）。因此没有语言开关，也没有两个入口——双语是同一内容的两个声道，不是两套课程。**人耳已验**（见第 3 组）。

> **P2 整体真机验收（2026-07-27，MI 8 SE / release 构建）**
>
> 五个页面逐一走查通过：位值工作台、百格板、合体求和、反向分解、等式槽。启动 714ms 无崩溃，全程无 Flutter 异常日志。
>
> **查出两个只有真机才会暴露的问题：**
>
> 1. **托盘里的积木缩略图整个不可见**（自 4.1 起就存在）。`PieceGlyph` 的每一小格是无子节点的 `DecoratedBox`，在 `Row` 默认的 `center` 对齐下拿到**松约束**，于是取 `constraints.smallest`——高度塌成 0，两个「源」看上去就是空白方块。已加 `crossAxisAlignment: stretch`。
>
>    **为什么测试没兜住**：既有用例只量了外层瓦片的尺寸——瓦片一直是对的，是里面什么都没有。新增 `piece_glyph_test.dart` 改为量**每一小格实际占了多大**，并在四种极端容器形状下验证；该用例在修复前失败 3 条。**教训：断言容器尺寸不等于断言内容可见。**
>
> 2. 三个拼搭台的棋盘顶在上边缘。格边长由宽度决定，棋盘往往比可用高度矮一截，下方留出一条空白带。已改为纵向居中。
>
> **仍然只能靠人和孩子判断的**：挤压回弹的力度、拖拽的跟手度、答错演示那 3.4 秒的停顿是否合适、烟花好不好看、连点时音效是否发闷。这些截图和测试都测不出来。
>
> **未验**：烟花实际观感（脚本没走到填满整行）、拖拽通道的手感（`adb input tap` 只能模拟点击，swipe 与真实手指的加速度曲线差别很大）。

## 5. P3 字母（约 1 周）

- [ ] 5.1 `assets/packs/letters.json` 与 `nouns.json`：26 字母的字母名、音素、对应名词与 emoji 码点
- [ ] 5.2 `lib/modules/letters/`：点击字母积木先播报字母名、再播报音素，两次可区分的播报
- [ ] 5.3 A is for Apple 交互版：选中字母后飞入多张名词图，**全部为正确答案**，点哪张都庆祝
- [ ] 5.4 字母轮廓填充：小方块填入字母模板，复用引擎吸附与双通道
- [ ] 5.5 大小写配对；配错时演示正确配对而非标记错误
- [ ] 5.6 拼自己的名字终关，目标名字来自可配置数据而非硬编码

## 6. P4 汉字（约 1 周）

- [ ] 6.1 `assets/packs/hanzi.json`：象形字（山田土木日月水火）、合体字（木+木=林、日+月=明、人+木=休）、`antonyms` 反义词对
- [ ] 6.2 `lib/modules/hanzi/`：象形动画——图片渐变为字形，可重播
- [ ] 6.3 部件加法：**直接复用 P2 的合体交互**，手势、吸附与反馈必须与加法模块完全一致；非法组合时部件返回原位，不标记错误
- [ ] 6.4 反义词跷跷板，配对数据取自内容包 `antonyms`
- [ ] 6.5 验证内容可扩展性：新增一个汉字内容包，确认新字在部件加法中直接可用且零代码改动
- [ ] 6.6 开始录制约 40 条真人语音（家人称谓、孩子名字、鼓励语、生日彩蛋台词），写入 `voice_overrides/` 验证即刻生效

## 7. P5 沙盒 + 家长端（约 1 周）

- [x] 7.1 `lib/modules/sandbox/`：空白拼搭台，跨内容域混搭，无关卡、无对错、无计分

> 提前实现的基础版本（原属 P5）：为验收 2.17 的手感而提前接上——积木引擎写完但没有任何界面用到它，手感无从验证，而 design.md 风险章节明确「宁可 P1 多花两天，也不要带着不稳的引擎进 P2」。当前具备：6×2 棋盘、按格宽自动补货的托盘、拖拽/点选双通道、无语义合体。**尚缺** 7.2 自发组合识别、7.3 作品保留与清空、跨内容域混搭（需 P2–P4 的内容积木）。
- [ ] 7.2 沙盒自发组合识别：自己摆出 `7+8=15` 或两个「木」时播报并庆祝；未构成组合时保持安静，不提示不催促
- [ ] 7.3 沙盒作品在离开与重进模块之间保留，并提供带动画的清空操作
- [ ] 7.4 30 层堆叠性能验证：拖拽与动画不掉帧
- [ ] 7.5 `lib/parent/gate.dart`：长按 3 秒 + 两位数乘法家长门，挡住全部设置入口；长按不足或答错则返回儿童端
- [ ] 7.6 `lib/parent/recorder.dart`：逐条录音覆盖，`AudioEncoder.wav` 写入 `voice_overrides/`，支持试听、重录、删除；删除后回落 TTS；无麦克风权限时入口禁用且不影响其余功能
- [ ] 7.7 时长上限：`WidgetsBindingObserver` + 墙钟时间戳持久化累加，**不用 `Timer`**；切后台、杀进程重启均不重置；跨自然日归零
- [ ] 7.8 达上限时积木「困了」+ 温柔谢幕动画，不硬锁屏、不弹强制对话框
- [ ] 7.9 `lib/parent/settings.dart`：中英模式开关、各模块启用开关，立即生效并持久化
- [ ] 7.10 `lib/parent/dashboard.dart`：今日玩了什么 / 哪些内容还生疏，仅用本机存档，零网络请求
- [ ] 7.11 家长区素材署名页（OpenMoji CC BY-SA 4.0）、隐私政策页（不收集不上传任何数据）、Android 屏幕固定与 iOS 引导式访问的使用引导

## 8. P6 生日彩蛋 + 打磨（约 3–5 天）

- [ ] 8.1 `lib/birthday/`：首启一次性播放 + 固定入口可重播；播放中触摸可跳过且不卡在中间状态
- [ ] 8.2 拼名字动画：姓名字母逐个飞入，姓名来自可配置数据
- [ ] 8.3 蜡烛环节：三块积木合体为 3 点燃三根蜡烛，复用引擎合体交互
- [ ] 8.4 吹气检测：`record.startStream(AudioEncoder.pcm16bits)` 在 Dart 侧算 RMS，**不写临时文件**；环境噪音不误触发
- [ ] 8.5 吹气降级路径：无麦克风权限或长时间无输入时改为点击熄灭，彩蛋必须完整可玩
- [ ] 8.6 家人相册：真实照片 + 家人真人语音走覆盖层，未录制时回落 TTS；照片未配置时优雅跳过该环节
- [ ] 8.7 全端合规自查：权限清单无网络声明、无第三方 SDK、无埋点；儿童端无任何跳出应用的入口
- [ ] 8.8 版权自查：应用名与角色名不含「Numberblocks / 数字积木」，角色造型与数字—颜色对应关系为原创
- [ ] 8.9 **Android 真机**手感终调：挤压回弹时长、音效延迟、吸附宽容度——这三项是 3 岁儿童唯一在意的东西。必须在 **release 构建**上调（debug 构建性能偏低，会误导判断）
- [ ] 8.10 交付前完整回归：`fvm flutter analyze` 无告警、`fvm flutter test` 全绿、CI 的 iOS 免签名构建通过、Android 真机装上 release APK 并跑完全部模块与彩蛋
- [ ] 8.11 生日前在 Android 设备上留存上一个可用的 release APK 作为回滚版本

## 9. 真实验收（让他玩，只看三个指标）

- [ ] 9.1 他**不需要你解释**就知道该干什么
- [ ] 9.2 他**主动重复**玩同一个模块
- [ ] 9.3 15 分钟结束时他**抗拒停止**，但谢幕动画能让他接受
- [ ] 9.4 任一指标不达标时：优先打磨手感而非增删功能
